## 🚚 Информационная система управления логистикой и доставкой

Полнофункциональная система управления логистикой и доставкой с использованием PostgreSQL, FastAPI и современного web-интерфейса.

---

## 📋 Содержание проекта

### 1. **database_schema.sql** - SQL схема БД
- 9 основных таблиц (сотрудники, клиенты, водители, транспорт, склады, маршруты, заказы, доставки)
- Индексы для оптимизации запросов
- 3 представления (Views) для аналитики
- Поддержка ACID транзакций

**Таблицы:**
- `employees` - Сотрудники компании
- `customers` - Клиенты для доставки
- `drivers` - Водители с лицензиями и рейтингами
- `vehicles` - Транспортные средства
- `warehouses` - Распределительные склады
- `delivery_routes` - Маршруты доставки
- `orders` - Заказы от клиентов
- `shipments` - Партии доставки (связь: заказ → водитель → маршрут)
- `deliveries` - Финальная доставка (детали доставки каждого груза)

### 2. **fastapi_backend.py** - REST API бэкенд
- FastAPI приложение с полной документацией (Swagger UI)
- SQLAlchemy ORM для работы с БД
- CRUD операции для всех сущностей
- Аналитические эндпоинты

**Основные эндпоинты:**
```
GET  /api/orders              - Список заказов
POST /api/orders              - Создать заказ
GET  /api/orders/{id}         - Информация о заказе
PUT  /api/orders/{id}/status  - Изменить статус

GET  /api/shipments           - Список доставок
POST /api/shipments           - Создать доставку
PUT  /api/shipments/{id}/status - Обновить статус

GET  /api/customers           - Список клиентов
POST /api/customers           - Создать клиента

GET  /api/drivers             - Список водителей
PUT  /api/drivers/{id}/availability - Изменить доступность

GET  /api/analytics/revenue   - Финансовая аналитика
GET  /api/analytics/driver-performance - Статистика водителей
GET  /api/health              - Health check
```

### 3. **interface_web.html** - Веб-интерфейс
- Отзывчивый дизайн (responsive)
- Реал-тайм обновление данных
- Модальные окна для создания/редактирования
- Таблицы с фильтрацией и поиском
- Аналитическая панель с KPI

**Функции интерфейса:**
- 📊 Панель управления с KPI
- 📦 Управление заказами (CRUD)
- 🚛 Управление доставками
- 👥 Управление клиентами
- 🚗 Управление водителями
- 🔧 Управление транспортом
- 📈 Аналитика и отчеты

### 4. **populate_database.py** - Скрипт заполнения БД
- Загрузка реальных данных (8 сотрудников, 8 клиентов, 4 водителя, 8 ТС, и т.д.)
- 20 заказов, 15 партий доставки, 20 доставок
- Случайные данные для тестирования
- Статистика загруженных данных

---

## 🛠️ Требования

```
Python 3.8+
PostgreSQL 12+
pip (Python package manager)
```

---

## 📦 Установка

### 1. Подготовка PostgreSQL

```bash
# Создание БД (Linux/Mac)
sudo -u postgres psql -c "CREATE DATABASE logistics_db;"
sudo -u postgres psql -c "CREATE USER logistics WITH PASSWORD 'logistics_password';"
sudo -u postgres psql -c "ALTER ROLE logistics SET client_encoding TO 'utf8';"
sudo -u postgres psql -c "ALTER ROLE logistics SET default_transaction_isolation TO 'read committed';"
sudo -u postgres psql -c "ALTER ROLE logistics SET default_transaction_deferrable TO on;"
sudo -u postgres psql -c "GRANT ALL PRIVILEGES ON DATABASE logistics_db TO logistics;"

# Или создание вручную через pgAdmin
```

### 2. Загрузка схемы БД

```bash
# Linux/Mac
psql -U postgres -d logistics_db -f database_schema.sql

# Windows
psql -U postgres -d logistics_db -f "путь\до\database_schema.sql"

# Или через pgAdmin: Upload файл и выполнить
```

### 3. Установка Python зависимостей

```bash
# Создание виртуального окружения (рекомендуется)
python -m venv venv

# Активация (Linux/Mac)
source venv/bin/activate

# Активация (Windows)
venv\Scripts\activate

# Установка зависимостей
pip install fastapi uvicorn sqlalchemy psycopg2-binary python-multipart
```

### 4. Конфигурация подключения к БД

Отредактируйте в файлах:

**fastapi_backend.py (строка ~30):**
```python
DATABASE_URL = "postgresql://logistics:logistics_password@localhost:5432/logistics_db"
```

**populate_database.py (строка ~14-19):**
```python
DB_CONFIG = {
    'host': 'localhost',
    'port': 5432,
    'database': 'logistics_db',
    'user': 'logistics',
    'password': 'logistics_password'
}
```

### 5. Заполнение БД данными

```bash
python populate_database.py
```

**Вывод должен быть:**
```
🚚 Информационная система управления логистикой и доставкой
📦 Скрипт заполнения БД реальными данными
✓ Подключение к БД успешно
📝 Заполнение таблицы сотрудников...
  ✓ Добавлен сотрудник: Иван Петров
  ✓ Добавлен сотрудник: Мария Сидорова
...
✅ ВСЕ ДАННЫЕ УСПЕШНО ЗАГРУЖЕНЫ В БД!
```

---

## 🚀 Запуск

### 1. Запуск API сервера

```bash
python -m uvicorn fastapi_backend:app --reload --host 0.0.0.0 --port 8000
```

**Вывод:**
```
INFO:     Uvicorn running on http://0.0.0.0:8000
INFO:     Application startup complete
```

### 2. Открытие веб-интерфейса

```
Откройте в браузере: file:///путь/до/interface_web.html
или
http://localhost:8000/openapi (Swagger UI документация API)
```

### 3. Проверка API здоровья

```bash
curl http://localhost:8000/api/health
```

---

## 📊 Использование

### Веб-интерфейс

1. **Панель управления** - Общая статистика и KPI
2. **Заказы** - Просмотр, создание, изменение статуса
3. **Доставки** - Управление партиями доставки
4. **Клиенты** - Добавление и управление клиентами
5. **Водители** - Список водителей и их рейтинги
6. **Транспорт** - Управление парком ТС
7. **Аналитика** - KPI и производительность

### API примеры

**Получить все заказы:**
```bash
curl http://localhost:8000/api/orders
```

**Создать новый заказ:**
```bash
curl -X POST http://localhost:8000/api/orders \
  -H "Content-Type: application/json" \
  -d '{
    "order_number": "ORD-2024-00001",
    "customer_id": 1,
    "warehouse_id": 1,
    "order_date": "2024-01-15",
    "delivery_date": "2024-01-20",
    "cost": 5000,
    "priority": "High",
    "status": "Pending"
  }'
```

**Обновить статус заказа:**
```bash
curl -X PUT http://localhost:8000/api/orders/1/status \
  -H "Content-Type: application/json" \
  -d '{"status": "Delivered"}'
```

**Получить аналитику:**
```bash
curl http://localhost:8000/api/analytics/revenue
curl http://localhost:8000/api/analytics/driver-performance
```

---

## 🗄️ Структура БД

```
employees (8)
├── drivers (4)
│   └── shipments (15)
│       ├── orders (20)
│       │   └── customers (8)
│       ├── vehicles (8)
│       └── delivery_routes (8)
└── warehouses (4)
    └── deliveries (20)
```

---

## 📈 Статистика загруженных данных

```
Сотрудников: 8
Клиентов: 8
Водителей: 4
Транспортных средств: 8
Складов: 4
Маршрутов: 8
Заказов: 20
Партий доставки: 15
Доставок: 20
```

---

## 🔒 Безопасность

⚠️ **Для production:**
1. Измените пароли в конфигурации
2. Используйте переменные окружения для чувствительных данных
3. Добавьте аутентификацию (JWT токены)
4. Используйте HTTPS
5. Настройте CORS правильно
6. Добавьте валидацию и санитизацию входных данных

---

## 🐛 Решение проблем

### Ошибка подключения к БД
```
psycopg2.OperationalError: could not connect to server
```
**Решение:** Проверьте, что PostgreSQL запущена и доступна на localhost:5432

### CORS ошибки при работе с API
**Решение:** CORS уже настроен в FastAPI, проверьте адреса хостов

### HTML файл не загружается
**Решение:** Откройте через `file://` протокол или запустите локальный HTTP сервер:
```bash
python -m http.server 8001
# Откройте http://localhost:8001/interface_web.html
```

---

## 📚 Дополнительные ресурсы

- [FastAPI документация](https://fastapi.tiangolo.com/)
- [SQLAlchemy документация](https://docs.sqlalchemy.org/)
- [PostgreSQL документация](https://www.postgresql.org/docs/)
- [Swagger/OpenAPI](https://swagger.io/)

---

## 📝 Лицензия

MIT License - Свободное использование в учебных целях

---

## ✅ Чек-лист готовности

- [x] SQL схема БД (9 таблиц, индексы, представления)
- [x] FastAPI бэкенд (CRUD, аналитика, валидация)
- [x] Веб-интерфейс (HTML/CSS/JS, отзывчивый дизайн)
- [x] Скрипт заполнения БД (реальные данные, 20+ заказов)
- [x] Документация установки и использования
- [x] API документация (Swagger UI)
- [x] Примеры использования

**Проект готов к использованию! 🎉**
