## 📐 Архитектура системы управления логистикой и доставкой

---

## 🏗️ Трёхслойная архитектура

```
┌─────────────────────────────────────────────────────┐
│                  PRESENTATION LAYER                 │
│              (interface_web.html)                   │
│                                                     │
│  ┌─────────────────────────────────────────────┐  │
│  │ HTML/CSS/JavaScript Web Interface           │  │
│  │ - Dashboard, Tables, Modals, Forms          │  │
│  │ - Real-time updates via FETCH API           │  │
│  │ - Responsive design (Mobile/Desktop)        │  │
│  └─────────────────────────────────────────────┘  │
└──────────────────────┬──────────────────────────────┘
                       │ HTTP/JSON
                       │ REST API
                       ▼
┌─────────────────────────────────────────────────────┐
│                  APPLICATION LAYER                  │
│               (fastapi_backend.py)                  │
│                                                     │
│  ┌─────────────────────────────────────────────┐  │
│  │ FastAPI RESTful API                         │  │
│  │ ├── CRUD Endpoints                          │  │
│  │ ├── Authentication (optional)               │  │
│  │ ├── Data Validation (Pydantic)              │  │
│  │ └── Analytics Endpoints                     │  │
│  └─────────────────────────────────────────────┘  │
│                                                     │
│  ┌─────────────────────────────────────────────┐  │
│  │ SQLAlchemy ORM                              │  │
│  │ ├── Model Definitions                       │  │
│  │ ├── Relationship Mapping                    │  │
│  │ └── Transaction Management                  │  │
│  └─────────────────────────────────────────────┘  │
└──────────────────────┬──────────────────────────────┘
                       │ SQL/psycopg2
                       │ Database Protocol
                       ▼
┌─────────────────────────────────────────────────────┐
│                    DATA LAYER                       │
│               (PostgreSQL Database)                 │
│                                                     │
│  ┌──────────────┐  ┌──────────────┐               │
│  │  Tables      │  │  Indexes     │               │
│  │              │  │              │               │
│  │ employees    │  │ idx_orders_* │               │
│  │ customers    │  │ idx_shipment │               │
│  │ drivers      │  │ idx_drivers  │               │
│  │ vehicles     │  │ idx_deliverie│               │
│  │ warehouses   │  │              │               │
│  │ routes       │  └──────────────┘               │
│  │ orders       │                                  │
│  │ shipments    │  ┌──────────────┐               │
│  │ deliveries   │  │   Views      │               │
│  └──────────────┘  │              │               │
│                    │ order_stats  │               │
│                    │ driver_activity│              │
│                    │ warehouse_occ │               │
│                    └──────────────┘               │
└─────────────────────────────────────────────────────┘
```

---

## 📊 Диаграмма сущностей (ER Diagram)

```
                         ┌─────────────┐
                         │  employees  │
                         ├─────────────┤
                         │ employee_id │◄──┐
                         │ full_name   │   │
                         │ position    │   │
                         │ email       │   │
                         │ phone       │   │
                         │ hire_date   │   │
                         │ salary      │   │
                         └─────────────┘   │
                                          │
                  ┌───────────────────┬───┴──────┐
                  │                   │          │
                  ▼                   ▼          ▼
           ┌────────────┐      ┌────────┐  ┌──────────┐
           │  drivers   │      │warehs  │  │ manager  │
           ├────────────┤      └────────┘  └──────────┘
        ┌──│ employee_id│◄──┐
        │  │ license_#  │   │
        │  │ rating     │   │
        │  └────────────┘   │
        │                   │
        │  ┌─────────────┐  │
        │  │  customers  │  │
        │  ├─────────────┤  │
        │  │customer_id  │  │
        │  │company_name │  │
        │  │contact_pers │  │
        │  │ email       │  │
        │  └─────────────┘  │
        │        │          │
        │        ▼          │
        │  ┌──────────────┐ │
        │  │   orders     │ │
        │  ├──────────────┤ │
        │  │order_id      │ │
        │  │order_number  │ │
        │  │order_date    │ │
        │  │delivery_date │ │
        │  │status        │ │
        │  │cost          │ │
        │  └──────────────┘ │
        │        │          │
        │        ▼          │
        │  ┌──────────────┐ │
        │  │  shipments   │ │
        │  ├──────────────┤ │
        │  │shipment_id   │ │
        │  │shipment_#    │ │
        │  │vehicle_id    │ │
        │  │driver_id     │◄┘
        │  │route_id      │
        │  │status        │
        │  └──────────────┘
        │        │
        └────────┤────────────┬──────────────┐
                 │            │              │
                 ▼            ▼              ▼
            ┌────────┐  ┌────────────┐  ┌──────────┐
            │vehicles│  │   routes   │  │deliveries│
            ├────────┤  ├────────────┤  ├──────────┤
            │vehicle │  │route_id    │  │delivery_id
            │_id     │  │route_name  │  │shipment_id
            │license │  │start_loc   │  │recipient
            │_plate  │  │end_loc     │  │address
            │type    │  │distance_km │  │status
            │capacity│  │duration_hrs│  │attempts
            └────────┘  └────────────┘  └──────────┘
```

---

## 🔄 Поток данных и бизнес-процесс

```
1. СОЗДАНИЕ ЗАКАЗА
   ┌─────────────┐
   │  Customer   │
   │ Submits     │
   │ Order Form  │
   └──────┬──────┘
          │ HTTP POST /api/orders
          ▼
   ┌──────────────┐
   │ FastAPI API  │
   │ - Validate   │
   │ - Insert DB  │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐      ┌─────────────────┐
   │   orders     │─────►│ Status: Pending │
   │   table      │      └─────────────────┘
   └──────────────┘


2. СОЗДАНИЕ ПАРТИИ ДОСТАВКИ
   ┌──────────────┐
   │  Dispatcher  │
   │ Selects      │
   │ Order + Truck│
   │ + Driver     │
   └──────┬───────┘
          │ HTTP POST /api/shipments
          ▼
   ┌──────────────┐
   │ FastAPI API  │
   │ - Validate   │
   │ - Link data  │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │  shipments   │   Связывает:
   │   table      │   - Order
   └──────┬───────┘   - Vehicle
          │          - Driver
          │          - Route
          ▼
   ┌─────────────────────┐
   │ Status: Pending ──► │
   │ (ready for pickup)  │
   └─────────────────────┘


3. ОТПРАВКА И ДОСТАВКА
   ┌──────────────┐
   │   Driver     │
   │  Picks up    │
   │  and Departs │
   └──────┬───────┘
          │ PUT /api/shipments/{id}/status
          │ Status: In Transit
          ▼
   ┌──────────────┐
   │ Traveling... │◄────────────────┐
   │              │                 │
   │ Real-time    │ Track location  │
   │ tracking     │ (optional)      │
   └──────┬───────┘                 │
          │                         │
          ▼                         │
   ┌──────────────────────────────────────┐
   │         Create Delivery Record       │
   │  POST /api/deliveries                │
   │  - Recipient info                    │
   │  - Expected arrival                  │
   │  - Signature required? (Y/N)         │
   └──────┬───────────────────────────────┘
          │
          ▼
   ┌──────────────┐
   │   Driver     │
   │  Completes   │
   │  Delivery    │
   └──────┬───────┘
          │ PUT /api/deliveries/{id}/complete
          │ Status: Delivered
          ▼
   ┌──────────────────────┐
   │ Shipment Status:     │
   │ Delivered ✓          │
   │ + Completion time    │
   │ + Signature          │
   └──────────────────────┘


4. АНАЛИТИКА
   ┌──────────────┐
   │   Manager    │
   │  Views       │
   │  Dashboard   │
   └──────┬───────┘
          │ GET /api/analytics/*
          ▼
   ┌────────────────────────────┐
   │ Real-time metrics:         │
   │ - Total orders: 20         │
   │ - Delivered: 15            │
   │ - In transit: 3            │
   │ - Revenue: ₽345,000        │
   │ - Driver performance       │
   │ - Route efficiency         │
   └────────────────────────────┘
```

---

## 🗄️ Структура таблиц и связи

### ОСНОВНЫЕ СУЩНОСТИ

**employees (Сотрудники)**
```sql
┌──────────────────────────────────┐
│ PK: employee_id                  │
│ full_name                        │
│ position (Manager, Driver, etc)  │
│ email (UNIQUE)                   │
│ phone                            │
│ hire_date                        │
│ salary                           │
│ is_active (Boolean)              │
└──────────────────────────────────┘
         │
         ├─ FK: drivers.employee_id
         └─ FK: warehouses.manager_id
```

**customers (Клиенты)**
```sql
┌──────────────────────────────────┐
│ PK: customer_id                  │
│ company_name                     │
│ contact_person                   │
│ email (UNIQUE, nullable)         │
│ phone                            │
│ city                             │
│ address                          │
│ postal_code                      │
│ registration_date                │
│ is_active                        │
└──────────────────────────────────┘
         │
         └─ FK: orders.customer_id
```

**drivers (Водители)**
```sql
┌──────────────────────────────────┐
│ PK: driver_id                    │
│ FK: employee_id (UNIQUE)         │
│ license_number (UNIQUE)          │
│ license_expiry_date              │
│ experience_years                 │
│ rating (1.0 - 5.0)              │
│ is_available (Boolean)           │
└──────────────────────────────────┘
         │
         └─ FK: shipments.driver_id
```

**vehicles (Транспортные средства)**
```sql
┌──────────────────────────────────┐
│ PK: vehicle_id                   │
│ license_plate (UNIQUE)           │
│ vehicle_type (Car/Van/Truck)     │
│ brand, model                     │
│ year                             │
│ capacity_kg                      │
│ capacity_cubic_m                 │
│ mileage                          │
│ last_maintenance (Date)          │
│ is_available                     │
└──────────────────────────────────┘
         │
         └─ FK: shipments.vehicle_id
```

**warehouses (Склады)**
```sql
┌──────────────────────────────────┐
│ PK: warehouse_id                 │
│ warehouse_name                   │
│ city                             │
│ address                          │
│ postal_code                      │
│ FK: manager_id (employees)       │
│ capacity_items (max)             │
│ current_items (tracking)         │
│ phone, email                     │
│ is_active                        │
└──────────────────────────────────┘
         │
         └─ FK: orders.warehouse_id
```

**delivery_routes (Маршруты)**
```sql
┌──────────────────────────────────┐
│ PK: route_id                     │
│ route_name                       │
│ start_location                   │
│ end_location                     │
│ distance_km                      │
│ estimated_duration_hours         │
│ is_active                        │
└──────────────────────────────────┘
         │
         └─ FK: shipments.route_id
```

### ТРАНЗАКЦИОННЫЕ ТАБЛИЦЫ

**orders (Заказы)**
```sql
┌──────────────────────────────────┐
│ PK: order_id                     │
│ order_number (UNIQUE)            │
│ FK: customer_id                  │
│ FK: warehouse_id                 │
│ order_date                       │
│ delivery_date                    │
│ description                      │
│ total_weight_kg                  │
│ total_volume_cubic_m             │
│ status (Pending→Processing→      │
│         Shipped→Delivered)       │
│ priority (Low/Normal/High/Urgent)│
│ cost (Decimal)                   │
│ notes                            │
│ created_at, updated_at (Timestamps)
└──────────────────────────────────┘
         │
         └─ FK: shipments.order_id
```

**shipments (Партии доставки)**
```sql
┌──────────────────────────────────┐
│ PK: shipment_id                  │
│ shipment_number (UNIQUE)         │
│ FK: order_id                     │
│ FK: vehicle_id                   │
│ FK: driver_id                    │
│ FK: route_id                     │
│ departure_time (Timestamp)       │
│ expected_arrival_time (Timestamp)│
│ actual_arrival_time (Timestamp)  │
│ status (Pending→In Transit→      │
│         Delivered→Delayed/Failed)│
│ distance_traveled_km             │
│ fuel_consumed_liters             │
│ cost                             │
│ created_at, updated_at           │
└──────────────────────────────────┘
         │
         └─ FK: deliveries.shipment_id
```

**deliveries (Доставки)**
```sql
┌──────────────────────────────────┐
│ PK: delivery_id                  │
│ FK: shipment_id                  │
│ recipient_name                   │
│ recipient_phone                  │
│ recipient_address                │
│ recipient_city                   │
│ delivery_time (Timestamp)        │
│ signature_required (Boolean)     │
│ signature_obtained (Boolean)     │
│ signature_date (Timestamp)       │
│ delivery_notes                   │
│ status (Pending→In Transit→      │
│         Delivered→Failed→        │
│         Reattempt)               │
│ attempts (retry counter)         │
│ created_at, updated_at           │
└──────────────────────────────────┘
```

---

## 🔗 Связи между таблицами

```
employees
  │
  ├─────────────────────────────────┬─────────────────┐
  │                                 │                 │
  ├─ (1:1) ─────────────────┬────────────────┬──────────────────┐
  │                         │                │                  │
  ▼                         ▼                ▼                  ▼
drivers              warehouses         (manager)          (normal employee)
  │                        │
  │                        │
  └─ (1:N) ────────┬──────┴─ (1:N) ────────┐
                   │                       │
                   ▼                       ▼
              shipments              orders
                   │                   │
                   ├─ (N:1) ◄──────────┤
                   │                   │
                   ├─────────────┬──────┘
                   │             │
                   ├─ (N:1) ─────┴──► customers
                   │
                   ├─ (N:1) ──────────► vehicles
                   │
                   ├─ (N:1) ──────────► delivery_routes
                   │
                   └─ (1:N) ──────────┐
                                      │
                                      ▼
                                  deliveries

Индекс: INDEX idx_orders_customer ON orders(customer_id)
Индекс: INDEX idx_shipments_vehicle ON shipments(vehicle_id)
Индекс: INDEX idx_deliveries_status ON deliveries(status)
```

---

## 📈 API Endpoints Map

```
REST API (/api)
│
├── /orders (Orders Management)
│   ├── GET    /orders               - List all orders
│   ├── GET    /orders/{id}          - Get single order
│   ├── POST   /orders               - Create new order
│   ├── PUT    /orders/{id}          - Update order
│   ├── PUT    /orders/{id}/status   - Update status
│   └── DELETE /orders/{id}          - Delete order
│
├── /shipments (Shipment Management)
│   ├── GET    /shipments            - List shipments
│   ├── GET    /shipments/{id}       - Get shipment
│   ├── POST   /shipments            - Create shipment
│   ├── PUT    /shipments/{id}/status- Update status
│   └── DELETE /shipments/{id}       - Delete
│
├── /customers (Customer Management)
│   ├── GET    /customers            - List customers
│   ├── GET    /customers/{id}       - Get customer
│   ├── POST   /customers            - Create customer
│   └── PUT    /customers/{id}       - Update customer
│
├── /drivers (Driver Management)
│   ├── GET    /drivers              - List drivers
│   ├── GET    /drivers/{id}         - Get driver
│   ├── POST   /drivers              - Create driver
│   └── PUT    /drivers/{id}/availability - Update availability
│
├── /vehicles (Vehicle Management)
│   ├── GET    /vehicles             - List vehicles
│   ├── GET    /vehicles/{id}        - Get vehicle
│   ├── POST   /vehicles             - Create vehicle
│   └── PUT    /vehicles/{id}        - Update vehicle
│
├── /deliveries (Delivery Management)
│   ├── GET    /deliveries           - List deliveries
│   ├── POST   /deliveries           - Create delivery
│   ├── PUT    /deliveries/{id}/complete - Mark as delivered
│   └── GET    /deliveries/status/{status} - By status
│
├── /analytics (Analytics & Reports)
│   ├── GET    /analytics/orders-by-status - Order statistics
│   ├── GET    /analytics/revenue         - Revenue metrics
│   ├── GET    /analytics/driver-performance - Driver stats
│   ├── GET    /analytics/vehicle-usage    - Vehicle stats
│   └── GET    /analytics/warehouse-occupancy - Warehouse stats
│
└── /health (System Health)
    └── GET    /health               - API status check
```

---

## 🎯 Основные KPI и метрики

```
Dashboard KPI:
┌─────────────────────────────────┐
│ Total Orders        20           │ CRUD операции
│ Delivered Orders    15 (75%)     │ Мониторинг статуса
│ In Transit Orders   3 (15%)      │ Отслеживание
│ Pending Orders      2 (10%)      │
├─────────────────────────────────┤
│ Total Revenue       ₽345,000     │ Финансовая аналитика
│ Avg Order Cost      ₽17,250      │
│ Total Shipments     15           │
│ Avg Shipment Cost   ₽5,800       │
├─────────────────────────────────┤
│ Active Drivers      4            │ Управление персоналом
│ Avg Driver Rating   4.75 / 5.0   │
│ Trips Per Driver    3.75         │
├─────────────────────────────────┤
│ Available Vehicles  6 / 8        │ Управление флотом
│ Avg Vehicle Load    78%          │
│ Warehouse Occupancy 62%          │
└─────────────────────────────────┘
```

---

## 💾 Резервное копирование и восстановление

```
Backup Strategy:
┌─────────────────┐
│  Daily Backup   │─┐
│  (Full Dump)    │ └──► Cloud Storage / NAS
├─────────────────┤
│  Transaction    │
│  Logs (WAL)     │
└─────────────────┘
         │
         ▼
┌─────────────────────────────┐
│   PITR Capability           │
│   (Point-In-Time Recovery)  │
└─────────────────────────────┘

Restore Scenario:
┌──────────────┐
│  Data Loss   │
└──────┬───────┘
       │
       ▼
┌──────────────────────────────┐
│ 1. Restore from Backup       │
│ 2. Apply transaction logs    │
│ 3. Verify data integrity     │
│ 4. Bring online              │
└──────────────────────────────┘
```

---

## 🔒 Безопасность и масштабируемость

```
Security Layers:
┌──────────────────────────────────┐
│ Application Layer                │
│ - Input Validation (Pydantic)    │
│ - SQL Injection Prevention (ORM) │
│ - CORS Configuration             │
├──────────────────────────────────┤
│ Database Layer                   │
│ - User Roles & Permissions       │
│ - Row-Level Security             │
│ - Encryption at rest (optional)  │
├──────────────────────────────────┤
│ Network Layer                    │
│ - HTTPS/TLS                      │
│ - Firewall rules                 │
│ - VPN (for internal)             │
└──────────────────────────────────┘

Scalability:
┌───────────────────────────────────┐
│ Horizontal Scaling                │
│ ├─ Multiple API instances         │
│ ├─ Load Balancer (Nginx)          │
│ └─ Connection pooling             │
├───────────────────────────────────┤
│ Vertical Scaling                  │
│ ├─ Increase server resources      │
│ ├─ Database optimization          │
│ └─ Caching layer (Redis)          │
├───────────────────────────────────┤
│ Database Optimization             │
│ ├─ Indexes on frequently queries  │
│ ├─ Table partitioning             │
│ ├─ Query optimization             │
│ └─ Archiving old data             │
└───────────────────────────────────┘
```

---

**Проект готов к implementation! 🎉**
